version: '3'

services:
    db:
        image: postgres
    api:
        build: .
        command: dockerize -wait tcp://db:5432 python manage.py runserver 0.0.0.0:8000
        volumes:
            - .:/app:Z
        ports:
            - "8000:8000"
        depends_on:
            - db
